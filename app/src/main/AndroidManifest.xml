<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.gamesuitver2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameSuitVer2">
        <activity android:name=".ui.activity.auth.register.RegisterActivity"></activity>
        <activity android:name=".ui.activity.auth.login.LoginActivity" />
        <activity android:name=".ui.activity.game.GameActivity" />
        <activity android:name=".ui.activity.DashboardActivity" />
        <activity android:name=".ui.activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.LandingActivity" />
    </application>

</manifest>