<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.gamesuitver2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GameApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameSuitVer2">
        <activity android:name=".view.activity.profile.ProfileActivity" />
        <activity android:name=".view.activity.auth.register.RegisterActivity" />
        <activity android:name=".view.activity.auth.login.LoginActivity" />
        <activity android:name=".view.activity.game.GameActivity" />
        <activity android:name=".view.activity.DashboardActivity" />
        <activity android:name=".view.activity.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.LandingActivity" />
    </application>

</manifest>